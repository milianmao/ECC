interface:
  display_name: "Harness Implement"
  short_description: "Execute an approved plan with focused tests and checks"
  default_prompt: "Use $harness-implement to execute the approved plan and verify each batch."
