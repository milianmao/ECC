interface:
  display_name: "Harness Release"
  short_description: "Validate a release candidate before any external publish"
  default_prompt: "Use $harness-release to validate this release candidate without publishing it."
