---
description: Reproduce a failure and implement a regression-tested root-cause fix.
argument-hint: "[failure, logs, or reproduction]"
---

Load and follow the canonical `harness-debug` skill for this request:

$ARGUMENTS

Keep all workflow logic in the skill; this command is only a compatibility entry point.
