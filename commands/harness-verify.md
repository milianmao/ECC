---
description: Run relevant project checks and report exact evidence.
argument-hint: "[scope or check type]"
---

Load and follow the canonical `harness-verify` skill for this request:

$ARGUMENTS

Keep all workflow logic in the skill; this command is only a compatibility entry point.
