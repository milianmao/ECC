---
description: Validate a release candidate without publishing it.
argument-hint: "[version, artifact, or release target]"
---

Load and follow the canonical `harness-release` skill for this request:

$ARGUMENTS

Keep all workflow logic in the skill; this command is only a compatibility entry point.
