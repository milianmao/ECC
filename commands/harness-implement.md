---
description: Execute an approved plan with focused tests and checks.
argument-hint: "[approved plan, task, or scope]"
---

Load and follow the canonical `harness-implement` skill for this request:

$ARGUMENTS

Keep all workflow logic in the skill; this command is only a compatibility entry point.
